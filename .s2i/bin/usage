#!/bin/bash -e
 cat << EOF
This is the playframework 2.5 centos S2I Image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation

use application.cof as config file
n this file use environment variables if needed and define them in openshift, example : 
play.crypto.secret="changeme"
play.crypto.secret=${?APPLICATION_SECRET}

name of git package (eg app directory, eg app name) must be equal to name of binary generated by activator dist command
s2i build https://.... 
